PORT=3000
NPFL_URL=https://npfl.com.ng/fixtures-results/

# Admin credentials (defaults to hardcoded values if not specified)
ADMIN_USERNAME=suleimansaiduumar@gmail.com
ADMIN_PASSWORD=dancoach001

# Shared online database and object storage (required for multi-device production use)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# SUPABASE_KEY is accepted for backward compatibility, but keep service-role keys server-side only.
SUPABASE_KEY=

# Keep this server-side only. Used for all privileged admin/database writes.
# SUPABASE_ANON_KEY is optional for this Express architecture.

# Server-only API-Football key for live fixture data.
LIVE_FOOTBALL_API_KEY=
